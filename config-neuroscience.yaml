# Neuroscience vault. Fleet-wide defaults live in config-base.yaml —
# keys here override the base (deep merge, this file wins).
vault:
  path: /mnt/external/vault-neuroscience

data_dir: ./data-neuroscience
